[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mister-python-nizamova"
version = "0.1.0"
description = "Practice work with unittest"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
